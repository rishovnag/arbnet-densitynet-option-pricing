# --- core (required to train, evaluate, and run the test suite) -------------
torch>=2.0
numpy>=1.23
pandas>=1.5
scipy>=1.10

# --- bundled real data (required for scripts/train_nse.py + check_data.py) --
# openpyxl reads the 91-day T-bill auctions .xlsx; without it the risk-free
# rate silently falls back to a 6.5% constant.
openpyxl>=3.1

# --- optional: only for scripts/prepare_data.py (re-fetching raw NSE data) --
# requests>=2.28
# yfinance>=0.2
# tqdm>=4.64
